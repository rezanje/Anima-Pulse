// ============================================================
// Anima Pulse — Submit Konten page (server component shell)
// ============================================================
import { SubmitForm } from '@/components/submit-form';

export const metadata = { title: 'Submit Konten · Anima Pulse' };

export default function SubmitPage() {
  return <SubmitForm />;
}
